using Gtk 4.0;

template $ShutterWindow: ApplicationWindow {
  default-width: 600;
  default-height: 300;

  child: Label label {
    label: "Hello, World!";

    styles [
      "title-1",
    ]
  };
}
